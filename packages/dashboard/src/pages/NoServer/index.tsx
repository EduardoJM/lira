import React from 'react';

const NoServer: React.FC = () => (
    <div>
        <h1>O servidor não está ativo!</h1>
        <h3>Por favor, inicie o servidor e reinicie essa aplicação.</h3>
    </div>
);

export default NoServer;
